import { name } from '../../package.json'

export const EXT_NAME = name

// UI settings
export const BASE_URI = '/reporting'


// Backend settings
export const BASE_API_URI = 'https://etl.proba-v-mep.esa.int/'
